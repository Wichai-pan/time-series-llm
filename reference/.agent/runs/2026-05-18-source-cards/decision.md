# Decision

The three PDF sources were carded.

Processing status should move from `skimmed` to `carded` for:

- sdforger-2025-forging-time-series
- chatts-2025-aligning-time-series
- agentsense-2025-virtual-sensor-data

Project-level synthesis should be handled by `reference-project-synthesizer` or `literature-review-sprint` if the user wants to update memory claims/risks/actions.
